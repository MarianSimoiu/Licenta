'use strict';

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _enzyme = require('enzyme');

var _radioItems = require('../radioItems');

var _radioItems2 = _interopRequireDefault(_radioItems);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('<RadioItems />', function () {
    it('should return the 3 radios when 3 options are passed', function () {
        var options = [{ value: 'admin', label: 'Admin', disabled: true }, { value: 'user', label: 'User' }, { value: 'anonymous', label: 'Anonymous' }];
        var component = (0, _enzyme.mount)(_react2.default.createElement(_radioItems2.default, { options: options }));
        expect(component.children().length).toBe(3);
    });
    it('should pass the right props to the Radio component', function () {
        var option = [{ value: 'admin', label: 'Admin', disabled: true }];
        var component = (0, _enzyme.mount)(_react2.default.createElement(_radioItems2.default, {
            options: option,
            name: 'name-1',
            ariaDescribedby: 'error-1',
            onChange: function onChange() {}
        }));
        expect(component.find('Radio').props()).toEqual({
            value: option[0].value,
            label: option[0].label,
            disabled: option[0].disabled,
            ariaDescribedby: 'error-1',
            name: 'name-1',
            onChange: expect.any(Function),
            isChecked: false
        });
    });
});