'use strict';

var _getResizedColumns = require('../getResizedColumns');

var _getResizedColumns2 = _interopRequireDefault(_getResizedColumns);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('getResizedColumns', function () {
    it('should return the same columns passed', function () {
        var columns = [{ label: 'Name', field: 'name' }, { label: 'Email', field: 'email' }];
        expect((0, _getResizedColumns2.default)({ columns: columns })).toEqual([{ label: 'Name', field: 'name' }, { label: 'Email', field: 'email' }]);
    });
    it('should return the right columns', function () {
        var columns = [{ label: 'Name', field: 'name', computedWidth: 50 }, { label: 'Email', field: 'email', computedWidth: 50 }];
        expect((0, _getResizedColumns2.default)({ columns: columns, colIndex: 1, widthDelta: 30 })).toEqual([{ label: 'Name', field: 'name', computedWidth: 50 }, { label: 'Email', field: 'email', isResized: true, computedWidth: 80 }]);
    });
});