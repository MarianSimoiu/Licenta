'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.default = getStartRowKey;

var _getLastRowSelection = require('./getLastRowSelection');

var _getLastRowSelection2 = _interopRequireDefault(_getLastRowSelection);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function getStartRowKey(params) {
    var isMultiple = params.isMultiple,
        rowKeyValue = params.rowKeyValue,
        indexes = params.indexes,
        lastSelectedRowKey = params.lastSelectedRowKey;

    var fromRowKey = rowKeyValue;

    if (isMultiple) {
        fromRowKey = (0, _getLastRowSelection2.default)(indexes, lastSelectedRowKey) || rowKeyValue;
    }
    return fromRowKey;
}