'use strict';

var _isNotSameColumns = require('../isNotSameColumns');

var _isNotSameColumns2 = _interopRequireDefault(_isNotSameColumns);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('isNotSameColumns', function () {
    it('should return true when prevColumns and currentColumns does not have the same length ', function () {
        var prevColumns = [{}, {}];
        var currentColumns = [{}, {}, {}];
        expect((0, _isNotSameColumns2.default)(prevColumns, currentColumns)).toBe(true);
    });
    it('should return false when prevColumns and currentColumns are the same', function () {
        var prevColumns = [{ field: 'name', label: 'Name' }, { field: 'email', label: 'Email' }];
        var currentColumns = [{ field: 'name', label: 'Name' }, { field: 'email', label: 'Email' }];
        expect((0, _isNotSameColumns2.default)(prevColumns, currentColumns)).toBe(false);
    });
    it('should return true when prevColumns and currentColumns are the different', function () {
        var prevColumns = [{ field: 'name', label: 'Name' }, { field: 'email', label: 'Email' }];
        var currentColumns = [{ field: 'phone', label: 'Name' }, { field: 'email', label: 'Email' }];
        expect((0, _isNotSameColumns2.default)(prevColumns, currentColumns)).toBe(true);
    });
});