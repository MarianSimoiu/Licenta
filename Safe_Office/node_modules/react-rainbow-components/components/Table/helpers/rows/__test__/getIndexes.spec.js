'use strict';

var _getIndexes = require('../getIndexes');

var _getIndexes2 = _interopRequireDefault(_getIndexes);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var rows = [{ key: 'row-1' }, { key: 'row-2' }, { key: 'row-3' }];

describe('getIndexes', function () {
    it('should return an empty object when rows passed is not an array or an empty array', function () {
        var values = [[], {}, null, undefined, '', 'abcd', 1234, function () {}];
        values.forEach(function (value) {
            expect((0, _getIndexes2.default)(value)).toEqual({});
        });
    });
    it('should return the right indexes object when rows are passed', function () {
        expect((0, _getIndexes2.default)(rows)).toEqual({
            'row-1': { rowIndex: 0 },
            'row-2': { rowIndex: 1 },
            'row-3': { rowIndex: 2 }
        });
    });
});