'use strict';

var _getRowSelectionInputType = require('../getRowSelectionInputType');

var _getRowSelectionInputType2 = _interopRequireDefault(_getRowSelectionInputType);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('getRowSelectionInputType', function () {
    it('should return "radio" when maxRowSelection passed is 1', function () {
        var maxRowSelection = 1;
        expect((0, _getRowSelectionInputType2.default)(maxRowSelection)).toBe('radio');
    });
    it('should return "checkbox" when maxRowSelection passed is other than 1', function () {
        var values = [0, 2, 3, '0', 'abc', [], {}, undefined, null, function () {}];
        values.forEach(function (value) {
            expect((0, _getRowSelectionInputType2.default)(value)).toBe('checkbox');
        });
    });
});