'use strict';

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _enzyme = require('enzyme');

var _index = require('./../index');

var _index2 = _interopRequireDefault(_index);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('<Notification/>', function () {
    it('should fire an event when the close button is clicked', function () {
        var onClickFn = jest.fn();
        var component = (0, _enzyme.mount)(_react2.default.createElement(_index2.default, { title: 'Notification test', onRequestClose: onClickFn }));
        component.find('button.rainbow-notification_close').simulate('click');
        expect(onClickFn).toHaveBeenCalledTimes(1);
    });
    it('should set the prop size as "small" in ButtonIcon', function () {
        var component = (0, _enzyme.mount)(_react2.default.createElement(_index2.default, { title: 'Notification test' }));
        expect(component.find('ButtonIcon').prop('size')).toBe('small');
    });
    it('should have the right class names', function () {
        var component = (0, _enzyme.mount)(_react2.default.createElement(_index2.default, { title: 'Notification test' }));
        expect(component.find('div').prop('className')).toBe('rainbow-notification');
    });
    it('should have the right class names when have a custom class', function () {
        var component = (0, _enzyme.mount)(_react2.default.createElement(_index2.default, { title: 'Notification test', className: 'my-custom-class-name' }));
        expect(component.find('div').prop('className')).toBe('rainbow-notification my-custom-class-name');
    });
});