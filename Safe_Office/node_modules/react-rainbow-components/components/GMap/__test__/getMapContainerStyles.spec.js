'use strict';

var _getMapContainerStyles = require('./../getMapContainerStyles');

var _getMapContainerStyles2 = _interopRequireDefault(_getMapContainerStyles);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('getMapContainerStyles', function () {
    it('should return null if a ref is not passed', function () {
        expect((0, _getMapContainerStyles2.default)()).toBeNull();
    });
    it('should return null if ref passed does not have a parentNode', function () {
        var ref = {};
        expect((0, _getMapContainerStyles2.default)(ref)).toBeNull();
    });
    it('should return null if the parentNode the ref passed has an height set', function () {
        var ref = {
            parentNode: {
                style: {
                    height: 100
                }
            }
        };
        expect((0, _getMapContainerStyles2.default)(ref)).toBeNull();
    });
    it('should return an object with a minHeght of 300 if the parentNode of the ref passed does not have an height set', function () {
        var ref = {
            parentNode: {
                style: {
                    height: ''
                }
            }
        };
        expect((0, _getMapContainerStyles2.default)(ref)).toEqual({
            minHeight: 300
        });
    });
});