'use strict';

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _enzyme = require('enzyme');

var _checkboxList = require('../checkboxList');

var _checkboxList2 = _interopRequireDefault(_checkboxList);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var options = [{ value: 'admin', label: 'Admin', disabled: false }, { value: 'user', label: 'User', disabled: false }, { value: 'nobody', label: 'Anonymus', disabled: true }];

describe('<CheckboxList />', function () {
    it('should return the right amount of checkboxes ', function () {
        var component = (0, _enzyme.mount)(_react2.default.createElement(_checkboxList2.default, { options: options }));
        expect(component.children().length).toBe(3);
    });

    it('should pass the right props to the Checkbox component', function () {
        var component = (0, _enzyme.mount)(_react2.default.createElement(_checkboxList2.default, { options: [{ value: 'admin', label: 'Admin', disabled: false }] }));
        var checkbox = component.find('Checkbox');
        expect(checkbox.prop('value')).toBe('admin');
        expect(checkbox.prop('label')).toBe('Admin');
        expect(checkbox.prop('disabled')).toBe(false);
    });
});