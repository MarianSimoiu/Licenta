'use strict';

var _getYearsRange = require('./../getYearsRange');

var _getYearsRange2 = _interopRequireDefault(_getYearsRange);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('getYearsRange', function () {
    it('should return the right years range when only currentMonth is passed', function () {
        var params = {
            currentMonth: 3
        };
        expect((0, _getYearsRange2.default)(params).length).toEqual(201);
    });
    it('should return the right years range when minDate and maxDate are passed', function () {
        var params = {
            currentMonth: 3,
            minDate: new Date(2018, 0, 4),
            maxDate: new Date(2020, 0, 4)
        };
        expect((0, _getYearsRange2.default)(params)).toEqual([{ disabled: false, label: 2018, value: 2018 }, { disabled: false, label: 2019, value: 2019 }, { disabled: true, label: 2020, value: 2020 }]);
    });
});