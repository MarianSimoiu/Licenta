'use strict';

var _getInitialFocusedIndex = require('../getInitialFocusedIndex');

var _getInitialFocusedIndex2 = _interopRequireDefault(_getInitialFocusedIndex);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('getInitialFocusedIndex', function () {
    it('should return 0', function () {
        var options = [{ label: 'Paris' }, { label: 'New York' }, { label: 'San Fransisco' }];
        var values = [undefined, [], options];
        values.forEach(function (value) {
            expect((0, _getInitialFocusedIndex2.default)(value)).toBe(0);
        });
    });
    it('should return 1 when first option is type "header"', function () {
        var options = [{ label: 'Paris', type: 'header' }, { label: 'New York' }, { label: 'San Fransisco' }];
        expect((0, _getInitialFocusedIndex2.default)(options)).toBe(1);
    });
});